import { BatchPrerequisite } from './batch-prerequisite';

describe('BatchPrerequisite', () => {
  it('should create an instance', () => {
    expect(new BatchPrerequisite(0, 0, 0, "")).toBeTruthy();
  });
});