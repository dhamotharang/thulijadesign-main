import { TrainerEducationDetail } from './trainer-education-detail';

describe('TrainerEducationDetail', () => {
  it('should create an instance', () => {
    expect(new TrainerEducationDetail(0, 0, "", 0, 0, "", 0, "", "", "")).toBeTruthy();
  });
});