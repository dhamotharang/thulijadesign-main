import { MenuHeightDirective } from './menu-height.directive';

describe('MenuHeightDirective', () => {
	it('should create an instance', () => {
		const directive = new MenuHeightDirective(null);
		expect(directive).toBeTruthy();
	});
});
